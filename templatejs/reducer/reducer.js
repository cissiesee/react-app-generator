import * as actionTypes from "./<%=reducerName%>Types";
import { /*todo*/ } from "./<%=reducerName%>Action";
import <%=reducerName %>State from "./<%=reducerName%>State";

export default function <%=reducerName%>(state = <%=reducerName %>State, action) {
    switch (action.type) {
        // TODO
        default:
            return state;
    }
}